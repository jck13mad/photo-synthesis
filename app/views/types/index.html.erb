<h2>All Types</h2>
<br />
<ul>
    <% @types.each do |type| %>
        <li><%= link_to type.name, type_path(type) %></li>
    <% end %>
</ul>
<br />
<br />
<%= link_to 'Add new type', new_type_path %>