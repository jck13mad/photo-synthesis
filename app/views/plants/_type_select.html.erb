<%= f.label :type, "Type of Plant: " %>
<%= f.collection_select :type_id, Type.all, :id, :name, prompt: true %><br />

<%= f.fields_for :type do |t| %>
    <%= render partial: 'types/fields, locals: {f: t} %>
<% end %><br />
<br />